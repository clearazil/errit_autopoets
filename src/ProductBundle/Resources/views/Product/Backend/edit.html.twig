{% extends 'backend.html.twig' %}

{% block body %}
     <!--breadcrumbs-area start -->
    <div class="breadcrumbs-area">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <ul>
                        <li><a href="{{ path('backend') }}">Backend</a> <span><i class="fa fa-angle-right"></i></span></li>
                        <li><a href="{{ path('backend_product_index') }}">Products</a> <span><i class="fa fa-angle-right"></i></span></li>
                        <li>Edit <span><i class="fa fa-angle-right"></i></span></li>
                        <li>{{ product.id }}</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <!--breadcrumbs-area end -->
    
    <!--elements start-->
    <div class="elements">
        <div class="typhography text-left">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12 col-md-12">
                        <h1>Product edit</h1>

                        {{ form_start(edit_form) }}
                            {{ form_widget(edit_form) }}
                            <input class="btn btn-primary" type="submit" value="Edit" />
                            <a class="btn btn-info" href="{{ path('backend_product_index') }}">Back to the list</a>
                        {{ form_end(edit_form) }}

                    </div>
                </div>                        
            </div>            
        </div>
    </div>
    <!--elements end--> 
{% endblock %}
